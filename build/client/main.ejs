<% var title = 'Home' %>
<% var bodyClass = 'home' %>
<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>

    <!-- Import Google Icon Font -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <!-- Import Font Awesome -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css">
  <link href="/./css/styles.css" rel="stylesheet"></head>

  <body <% if (typeof bodyClass !== 'undefined') { %>class="<%= bodyClass %>"<% } %>>


    <nav>
  <div class="container nav-wrapper">
    <a href="/" class="brand-logo">I Need A Vacation</a>
    <ul id="nav-mobile" class="right hide-on-med-and-down">
      <% menu.forEach(function(item) { %>
        <li><a href="<%= item.link %>"><%= item.text %></a></li>
      <% }) %>
    </ul>
  </div>
</nav>



      <div class="container">
        <div class="card-panel white">
          <h1>I Need a Vacation</h1>
          <p>Search for a location in the search bar below! Have autocomplete results show up during search. Once you find the
            place you are looking for click on it in the results. A new screen will display with a list of popular places
            around the area you searched for. Each item in the list will contain information such as: image, place name,
            current hours, category, address, rating, price level, and a map showing the location. The map can be accessed
            be either clicking on the place's image or the marker icon to the right of the category.</p>

          <p>Want to search another place? Click the back button at the top right of the nav bar, or refresh the page.</p>
        </div>
        <nav>
          <div class="nav-wrapper">
            <div class="input-field">
              <input class="place-search" type="search" placeholder="Search for a place...">
              <label class="label-icon" for="search"><i class="material-icons">search</i></label>
              <i class="material-icons">close</i>
            </div>
          </div>
        </nav>
        <ul class="results__list autocomplete-content dropdown-content"></ul>
      </div>

<% var google = true %>
    <% if (typeof google !== 'undefined' && google) { %>
    <!-- Import Google Places API -->
    <script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCYlbzHkh8NuvIE8NVPdu0K5vgraQuoZbo&libraries=places"></script>
    <% } %>
  <script type="text/javascript" src="/js/vendor.d6c8bc16f62a3cf53108.js"></script><script type="text/javascript" src="/js/main.ab497510b4b91a4edf6b.js"></script></body>
</html>
